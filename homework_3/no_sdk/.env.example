# Per-variant .env for no_sdk/.
# Optional — if absent, the script falls back to ../.env (homework_2/.env)
# shared with sdk_codex. Copy to .env and fill in your key if you want this
# variant to use a different account or model than sdk_codex.

OPENAI_API_KEY=sk-...
# Optional: override the default model (gpt-5.4-mini)
# OPENAI_MODEL=
